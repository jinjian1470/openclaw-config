# runtime / secrets (do NOT sync)
keystore/
logs/
cache/
tmp/
sessions/
*.lock
*.log

# optional: keep node-specific config out of git
openclaw.json
openclaw.json.bak

# sensitive / node-specific (do NOT sync)
credentials/
identity/
devices/
devices/**
.openclaw/
update-check.json
